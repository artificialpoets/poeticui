# Project-level npm config. CI workflows set NPM_TOKEN to authenticate; locally,
# this file's auth line is harmless — your ~/.npmrc takes over for publishes.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
@artificialpoets:registry=https://registry.npmjs.org
